{
  "name": "loom-works",
  "description": "Lane / Task management with coupled documentation lifecycle",
  "version": "2.29.0",
  "userConfig": {
    "doc_folder_name": {
      "type": "string",
      "title": "Docs folder name",
      "description": "Folder for tasks.md, tasks/, reference/, current-task.md. Default: docs",
      "sensitive": false
    },
    "on_lane_spawned_hook": {
      "type": "string",
      "title": "On lane spawned hook",
      "description": "Path relative to project root, executed once after spawn-lane on the lane worktree root. Empty = noop. $1 = CWD = lane root. Env: LOOM_LANE, LOOM_WORKTREE, LOOM_PROJECT_ROOT.",
      "sensitive": false
    }
  }
}
